Gen object file from c. 
    # gcc object_name.o target.c

Date Type size rule.

    sizeof(short) <= sizeof(int) <= sizeof(long) <= sizeof(long long)

noraml
    short = 2 Bytes, int = 4 Bytes, long = 4 Bytes, long long = 8 Bytes.

sort_HW
       sorting high and weight.
       intput: numbers of n
       H.W.: mean heigh and weight
# How to search C function call
	e.g. man 3 printf... 
